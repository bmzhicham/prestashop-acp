/vendor/
composer.lock
.env
.env.local
config/parameters.php
/var/
/logs/
*.log
.idea/
.vscode/
.DS_Store

# Spike run state (per-machine, not portable — see spikes/*.php)
spikes/.*.state

# spikes/ is its own isolated Composer project, separate from the module's.
spikes/vendor/
